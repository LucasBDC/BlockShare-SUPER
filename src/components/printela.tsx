'use client'
export default function Printela(props: any){
     return(
        <main className="h-screen w-screen flex flex-col gap-4 items-center justify-center backgroundera">{props.children}</main>
    )
}


